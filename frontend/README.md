# Frontend (Next.js + shadcn-like)

Minimal Next.js app using Tailwind and shadcn-inspired components.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Defaults assume backend API at `http://localhost:8000`. Override with `NEXT_PUBLIC_API_BASE`.
